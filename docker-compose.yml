services:
  express-app:
    build: .
    container_name: la-mia-app-node
    restart: always  # <--- QUESTA è la riga magica per la VPS
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - .:/app
      - /app/node_modules